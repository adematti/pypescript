${demos/demo4.yaml:}:

main:
  $modules: [batch]

batch:
  $module_name: pypescript
  $module_class: BatchPipeline
  $execute: [like]
  $iter:
  $nprocs_per_task: 1
  #$datablock_duplicate:
  #  data.ysave:
  $[data.ysave]: $[data.ysave]
